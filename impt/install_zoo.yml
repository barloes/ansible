

- name: make sure clickhouse is enabled and started
  systemd:
    name: "zookeeper"
    state: stopped

- name: make sure services are enabled and started
  systemd:
    name: "zookeeper"
    daemon-reload: yes
    enabled: yes
    state: started