
- name: spawn new zookeeper
  shell: docker container stop zookeeper

- name: make sure services are enabled and started
  systemd:
    name: "zookeeper"
    daemon-reload: yes
    enabled: yes
    state: started